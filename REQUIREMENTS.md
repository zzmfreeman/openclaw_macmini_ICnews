
## UI 需求

### [2026-04-03] 页面显示推送时间
- 每次 git push 后，在页面显著位置展示本次推送时间（如 "页面更新: 2026-04-03 17:41 CST"）
- 目的：用户刷新后可快速确认看到的是否是最新版本
- 实现方式：每次简报生成时把 git push 完成时间写入 brief.json（如 `publishedAt` 字段），index.html 读取并展示

